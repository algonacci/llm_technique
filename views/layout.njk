<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{% block pageTitle %}LLM technique{% endblock %}</title>
    <style>
      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        margin: 0;
        padding: 1.5rem;
        background: #f6f6f9;
        color: #1f2933;
      }

      header {
        margin-bottom: 2rem;
      }

      nav ul {
        display: flex;
        gap: 1rem;
        list-style: none;
        padding: 0;
        margin: 0.5rem 0 0;
      }

      nav a,
      nav span {
        text-decoration: none;
        color: #2563eb;
        font-weight: 600;
      }

nav span {
  color: #111827;
}

main {
  background: #fff;
  border-radius: 0.75rem;
  padding: 1.5rem;
  box-shadow: 0 10px 25px rgba(15, 23, 42, 0.08);
}

.stream-output {
  min-height: 160px;
  background: #0f172a;
        color: #e2e8f0;
        border-radius: 0.75rem;
        padding: 1rem;
        margin-top: 1rem;
        white-space: pre-wrap;
        overflow-wrap: anywhere;
      }

  .think-block {
    margin-top: 1rem;
    border: 1px solid #cbd5f5;
    border-radius: 0.5rem;
    background: #eef2ff;
    color: #1f2933;
  }

  .think-block > summary {
    cursor: pointer;
    padding: 0.75rem 1rem;
        font-weight: 600;
        color: #1e3a8a;
      }

      .think-block > summary::marker {
        color: #1e3a8a;
  }

  .think-block pre {
    margin: 0;
    padding: 0.75rem 1rem 1rem;
    background: transparent;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
    color: inherit;
  }

  .think-block div {
    padding: 0 1rem 1rem;
    color: inherit;
  }

  .stream-caret {
    display: inline-block;
    width: 0.55rem;
    height: 1em;
    margin-left: 0.25rem;
    background: rgba(99, 102, 241, 0.6);
    animation: stream-caret-blink 1s step-end infinite;
    vertical-align: baseline;
  }

  .think-complete .stream-caret {
    display: none;
  }

  @keyframes stream-caret-blink {
    50% {
      opacity: 0;
    }
  }

  form.reasoning-form {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.75rem;
    background: linear-gradient(135deg, #eef2ff 0%, #e0e7ff 100%);
    border-radius: 0.75rem;
    padding: 1.25rem;
  }

  .reasoning-form .field {
    display: grid;
    gap: 0.5rem;
  }

  .reasoning-form label {
    font-weight: 600;
    color: #1f2933;
  }

  .reasoning-form textarea {
    width: 100%;
    min-height: 160px;
    border-radius: 0.75rem;
    padding: 1rem;
    border: 1px solid #c7d2fe;
    background: rgba(255, 255, 255, 0.95);
    font-size: 1rem;
    line-height: 1.5;
    resize: vertical;
    box-shadow: inset 0 1px 4px rgba(99, 102, 241, 0.1);
  }

  .reasoning-form textarea:focus {
    outline: none;
    border-color: #4f46e5;
    box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.2);
  }

  .reasoning-form .actions {
    display: flex;
    gap: 0.75rem;
    align-items: center;
    flex-wrap: wrap;
  }

  .reasoning-form button {
    background: linear-gradient(135deg, #6366f1 0%, #4338ca 100%);
    border: none;
    color: #fff;
    padding: 0.65rem 1.5rem;
    border-radius: 999px;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .reasoning-form .button-secondary {
    background: transparent;
    border: 1px solid rgba(79, 70, 229, 0.4);
    color: #3730a3;
  }

  .reasoning-form .button-secondary:hover:not(:disabled) {
    background: rgba(79, 70, 229, 0.08);
    box-shadow: none;
    transform: none;
  }

  .reasoning-form button:hover:not(:disabled) {
    transform: translateY(-1px);
    box-shadow: 0 12px 20px rgba(79, 70, 229, 0.25);
  }

  .reasoning-form button:disabled {
    cursor: not-allowed;
    opacity: 0.7;
  }

  form.upload-form {
    display: grid;
    gap: 0.75rem;
    background: linear-gradient(135deg, #f5f7ff 0%, #eef2ff 100%);
    border-radius: 0.75rem;
    padding: 1.25rem;
    margin-top: 1rem;
  }

  .upload-form .field {
    display: grid;
    gap: 0.5rem;
  }

  .upload-form label {
    font-weight: 600;
    color: #1f2933;
  }

  .upload-form input[type='file'],
  .upload-form input[type='text'] {
    padding: 0.6rem;
    border-radius: 0.5rem;
    border: 1px solid #cbd5f5;
    background: rgba(255, 255, 255, 0.9);
    font-size: 1rem;
  }

  .upload-form input[type='file']:focus,
  .upload-form input[type='text']:focus {
    outline: none;
    border-color: #4338ca;
    box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.15);
  }

  .upload-form .actions {
    display: flex;
    justify-content: flex-start;
  }

  .upload-form button {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    border: none;
    color: #fff;
    padding: 0.65rem 1.5rem;
    border-radius: 999px;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .upload-form button:hover {
    transform: translateY(-1px);
    box-shadow: 0 12px 20px rgba(16, 185, 129, 0.25);
  }

  .status-text {
    color: #4b5563;
  }

  .reasoning-form .toggle {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .toggle-label {
    font-weight: 600;
    color: #1f2933;
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.06em;
  }

  .toggle-state {
    font-weight: 600;
    color: #4338ca;
  }

  .toggle-state.is-off {
    color: #6b7280;
  }

  .switch {
    position: relative;
    display: inline-block;
    width: 52px;
    height: 28px;
  }

  .switch input {
    opacity: 0;
    width: 0;
    height: 0;
  }

  .slider {
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #c7d2fe;
    transition: 0.3s;
    border-radius: 999px;
  }

  .slider::before {
    position: absolute;
    content: '';
    height: 22px;
    width: 22px;
    left: 4px;
    bottom: 3px;
    background-color: white;
    transition: 0.3s;
    border-radius: 50%;
    box-shadow: 0 2px 6px rgba(79, 70, 229, 0.3);
  }

  .switch input:checked + .slider {
    background: linear-gradient(135deg, #6366f1 0%, #4338ca 100%);
  }

  .switch input:checked + .slider::before {
    transform: translateX(24px);
  }

  @media (min-width: 768px) {
    form.reasoning-form {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      align-items: start;
    }

    .reasoning-form .field {
      grid-column: span 2;
    }

    .reasoning-form .toggle {
      grid-column: span 2;
    }

    .reasoning-form .actions {
      grid-column: span 2;
    }
  }
</style>
  </head>
  <body>
    <header>
      <h1>{% block pageHeading %}LLM technique{% endblock %}</h1>
      <nav>
        <ul>
          {% set navItems = [
            { "label": "Home", "path": "/", "id": "home" },
            { "label": "ON/OFF Reasoning", "path": "/on_off_reasoning", "id": "on_off_reasoning" },
            { "label": "Stop Streaming Response", "path": "/stop_streaming_response", "id": "stop_streaming_response" },
            { "label": "Upload Image", "path": "/upload_image", "id": "upload_image" }
          ] %}
          {% for item in navItems %}
            <li>
              {% if item.id == pageId %}
                <span>{{ item.label }}</span>
              {% else %}
                <a href="{{ item.path }}">{{ item.label }}</a>
              {% endif %}
            </li>
          {% endfor %}
        </ul>
      </nav>
    </header>
    <main>
      {% block content %}{% endblock %}
    </main>
  </body>
</html>
